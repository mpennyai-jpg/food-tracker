---
description: Drop an address and prep the whole file — one discovery pass, one gap report, then every narrative skill
argument-hint: <address> [effective date] [--bg]
---

Run the `report-prep` skill on: $ARGUMENTS

Todd is starting to type this appraisal report. He wants the pre-report package
built while he works, and he wants **one** message telling him everything that is
missing before he has to go looking for it twice.

Read `C:\Users\toddp\.claude\skills\report-prep\SKILL.md` and follow it exactly.

Non-negotiable, even if the rest of the skill is somehow unavailable:

1. State the subject address on the first line of your reply before doing any
   work. That is house-rules section 9, the isolation rule.
2. Post the merged gap report **before** any analysis. One message, every gap,
   each one with what it costs him.
3. Never ask him for anything the tracker record or a keyless public lookup
   already carries.

If `--bg` appears in the arguments, post the gap report first and run the skills
as background agents so he can keep typing. Otherwise run them in the session.
